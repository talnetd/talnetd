# talnetd

```
$ whoami
tinaunglin — IT Guy
```

---

### `cat /proc/interests`

```
├── infrastructure/
│   ├── kubernetes the hard way
│   ├── bare-metal clusters on OVH
│   ├── proxmox homelab shenanigans
│   └── calico network policies at 2am
├── low-level/
│   ├── linux kernel modules
│   ├── overlay filesystems (OpenWRT)
│   └── L2 load balancing
├── monitoring/
│   ├── prometheus exporters
│   ├── grafana dashboards nobody asked for
│   └── alertmanager rules that actually work
└── current-obsessions/
    ├── MCP servers + LLM tooling
    ├── FPV drone builds 🚁
    └── mechanical keyboards (wallet crying)
```

### `git log --oneline`

```
US-20190124001-A1  feat: Layer 2 load balancer patent
firefox-l10n       feat: Burmese localization lead
mozilla-myanmar    feat: Mozilla Rep
openwrt-overlay    feat: kernel module for overlay fs
streaming-backend  feat: nginx-hls + ffmpeg for Myanmar Cast
```

### `neofetch`

```
OS:      Gentoo btw (and Debian in prod, I'm not insane)
Shell:   zsh + tmux
Editor:  neovim
WM:      i3 (fight me)
Term:    ghostty
```

### Stack I Actually Use Daily

```go
stack := map[string][]string{
    "infra":      {"k8s", "gke", "ovh", "proxmox", "pfsense"},
    "gitops":     {"argocd", "terraform", "ansible", "helm"},
    "data":       {"postgresql", "redis", "kafka"},
    "monitor":    {"prometheus", "grafana", "loki", "wazuh"},
    "languages":  {"go", "python", "bash", "c"},
}
```

### Currently Breaking

- Kubernetes cert-manager renewals
- Calico policies that block everything, including myself
- Home Proxmox cluster (again)
- Whatever LLM MCP integration I'm hacking on

---

<sub>🇲🇲 → 🇸🇬 | [tinaunglin@linux.com](mailto:tinaung.lin@icloud.com)</sub>
